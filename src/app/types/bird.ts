export interface Bird{
  id?: string;
  about: string;
  category: string;
  eieren: string;
  gewicht: string;
  lengte: string;
  verspreiding: string;
  latijns: string;
  image: string;
  name: string;
}
